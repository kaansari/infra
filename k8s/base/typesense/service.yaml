apiVersion: v1
kind: Service
metadata:
  name: typesense
  namespace: ceerat-data
  labels:
    app.kubernetes.io/name: typesense
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: typesense
  ports:
    - name: http
      port: 8108
      targetPort: http
