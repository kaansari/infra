apiVersion: v1
kind: Service
metadata:
  name: cloud-sql-proxy
  namespace: ceerat-data
  labels:
    app.kubernetes.io/name: cloud-sql-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cloud-sql-proxy
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
