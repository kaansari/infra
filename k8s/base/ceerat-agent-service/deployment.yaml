apiVersion: apps/v1
kind: Deployment
metadata:
  name: ceerat-agent-service
  namespace: ceerat-backend
  labels:
    app.kubernetes.io/name: ceerat-agent-service
    app.kubernetes.io/part-of: ceerat
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ceerat-agent-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ceerat-agent-service
        app.kubernetes.io/part-of: ceerat
    spec:
      containers:
        - name: ceerat-agent-service
          image: ceerat-agent-service
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8088
          env:
            - name: PORT
              value: "8088"
            - name: CEERAT_USER_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: ceerat-platform-config
                  key: CEERAT_API_BASE_URL
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ceerat-openai
                  key: OPENAI_API_KEY
            - name: OPENAI_MODEL
              valueFrom:
                secretKeyRef:
                  name: ceerat-openai
                  key: OPENAI_MODEL
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
